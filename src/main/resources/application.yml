server:
  port: 8091

spring:
  jpa:
    hibernate:
      ddl-auto: update
    database: POSTGRESQL
    show-sql: true
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/demo?currentSchema=public
    username: postgres
    password: postgres
    driverClassName: org.postgresql.Driver
    host: localhost
    port: 5432
